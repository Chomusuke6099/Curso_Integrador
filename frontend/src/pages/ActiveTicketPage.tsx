import {FormEvent,useEffect,useState} from 'react'; import {useNavigate,useParams} from 'react-router-dom'; import {api} from '../api/client'; import {TipificacionResponse} from '../types';
export default function ActiveTicketPage(){const{id}=useParams();const nav=useNavigate();const[tips,setTips]=useState<TipificacionResponse[]>([]);const[tipificacionId,setTipificacionId]=useState('');const[observacion,setObservacion]=useState('');const[error,setError]=useState('');useEffect(()=>{api.get<TipificacionResponse[]>('/catalogs/tipificaciones').then(setTips).catch(e=>setError(e.message))},[]);const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(!tipificacionId||observacion.trim().length<12){setError('Complete tipificación y observación.');return;}if(!confirm('Confirme el cierre del ticket.'))return;try{await api.put(`/tickets/${id}/close`,{tipificacionId:Number(tipificacionId),observacion});nav('/tickets')}catch(ex){setError(ex instanceof Error?ex.message:'No fue posible cerrar.')}};return <section className="card"><h1>Cierre de ticket</h1><form onSubmit={submit} className="form-grid"><label>Tipificación</label><select value={tipificacionId} onChange={e=>setTipificacionId(e.target.value)}><option value="">Seleccione</option>{tips.map(t=><option key={t.id} value={t.id}>{t.nombre}</option>)}</select><label>Observación</label><textarea value={observacion} onChange={e=>setObservacion(e.target.value)} maxLength={1000}/><small>{observacion.length}/1000</small>{error&&<div className="alert">{error}</div>}<button>Cerrar ticket</button></form></section>}

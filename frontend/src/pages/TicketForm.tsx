import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {api} from '../api/client'; import {TicketResponse} from '../types';
export default function TicketForm(){const nav=useNavigate();const[tipoCaso,setTipoCaso]=useState('CORPORATIVO');const[referencia,setReferencia]=useState('');const[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(referencia.trim().length<3){setError('Ingrese una referencia válida.');return;}try{const t=await api.post<TicketResponse>('/tickets',{tipoCaso,referencia});nav(`/tickets/${t.id}/close`)}catch(ex){setError(ex instanceof Error?ex.message:'No fue posible abrir.')}};return <section className="card"><h1>Apertura de ticket</h1><p>La hora oficial la registra el servidor.</p><form onSubmit={submit} className="form-grid"><label>Tipo</label><select value={tipoCaso} onChange={e=>setTipoCaso(e.target.value)}><option value="RECLAMO">Reclamo</option><option value="FACTURACION">Facturación</option><option value="SOPORTE">Soporte</option><option value="CORPORATIVO">Corporativo</option></select><label>Referencia</label><input value={referencia} onChange={e=>setReferencia(e.target.value)} maxLength={160}/>{error&&<div className="alert">{error}</div>}<button>Abrir ticket</button></form></section>}

import {Link,Outlet,useNavigate} from 'react-router-dom';
import {clearSession,currentName,currentRole} from '../api/client';
export default function Layout(){const nav=useNavigate();const rol=currentRole();return <div className="app-shell"><aside className="sidebar"><div className="brand">TMO</div><span className="muted">{currentName()}</span><span className="badge">{rol}</span><nav>{rol==='ANALISTA'&&<Link to="/tickets">Mis tickets</Link>}{rol==='ANALISTA'&&<Link to="/tickets/new">Abrir ticket</Link>}{rol!=='ANALISTA'&&<Link to="/dashboard">Dashboard</Link>}{rol!=='ANALISTA'&&<Link to="/reports">Reporte Excel</Link>}</nav><button className="secondary" onClick={()=>{clearSession();nav('/login')}}>Cerrar sesión</button></aside><main className="content"><Outlet/></main></div>}
